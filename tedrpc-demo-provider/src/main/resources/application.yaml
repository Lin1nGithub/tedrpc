server:
  port: 8081

tedrpc:
  zkServer: localhost:2181
  zkRoot: tedrpc

app:
  id: app1
  namespace: public
  env: dev
  metas: "{dc:'bj',gray:'flase',unity:'B001'}"

logging:
  level:
    root: error
    cn.theodore.tedrpc: info